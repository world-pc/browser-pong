function botMove(ball_x, ball_y, ball_vx, ball_va, bot_orientationy) {
    //returns the appropriate paddle state given ball position / velocity
    //and which side the bot is on.
    
    //if bot is on left side of screen
    if(bot_orientation < 0) {
    }
    else {
    }

}
